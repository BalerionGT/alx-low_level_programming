#include <stdio.h>

/**
 * main - check the code
 * @argc: the argument count
 * @argv: the argument vector
 *
 * Return: 0.
 */
int main(int argc, char **argv)
{
	printf("%s", argv[argc - 1]);
	printf("\n");
	return (0);
}
